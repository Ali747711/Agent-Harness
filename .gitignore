# deps / build
node_modules/
dist/
coverage/

# env & secrets
.env
.env.*

# os / editor
.DS_Store
*.log

# spike artifacts are throwaway (plan step 1)
spike/

# build + coverage artifacts
dist/
coverage/

# bun build scratch files (land in the repo root)
*.bun-build
